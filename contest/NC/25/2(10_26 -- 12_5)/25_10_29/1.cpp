#include<bits/stdc++.h>
using namespace std;
using vt = vector<int>;
using vll = vector<long long>;

void solve()
{
    int n,m;
    cin >> n >> m;
    vector grid<n,vector<char>(m);
    for (int i = 0;i < n;i++)
    {
        cin >> grid[i];
    }
    for (int i = 0;i < m;i++)
    {
        for (int j = 0;j < n;j++)
        {
            
        }
    }
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int _ = 1;
    // cin >> _;
    while (_ --) solve();

    return 0;
}